<h1><%= @venue.name %></h1>

<%= @venue.address %>
<%= @venue.extended_address %>

<h2>Upcoming Events</h2>

<ul>
  <% @venue.events.each do |event| %>
    <li>
      <%=event.title %>, 
      <%=event.datetime_local%>
    </li>
  <% end %>
</ul>